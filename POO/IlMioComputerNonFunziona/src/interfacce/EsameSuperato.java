package interfacce;
import core.*;

// I metodi delle nterfaccce di default sono public e abstract
// gli attributi sono public, static e final di default

public interface EsameSuperato {
     boolean addEsameSuperato(Studente s, Esame e);
}
