#include <stdio.h>
#include <stdlib.h>

int main(void) {
    char matricola[4];
    char nome[50];
    char media_voti;

    FILE *f_in;
    FILE *f_tmp;

    if(!(f_in = fopen("studenti.txt", "r")){
        exit(-1);
    }

    if(f_tmp = fopen("studenti.txt", "w")) {

    }

    fscanf(f_in, )

